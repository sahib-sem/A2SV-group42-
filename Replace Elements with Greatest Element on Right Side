class Solution:
    def replaceElements(self, arr: List[int]) -> List[int]:
        mx=-1
        i=len(arr)-1
        while i>=0:
            temp=arr[i]
            arr[i]=mx
            if temp>mx:
                mx=temp
            i-=1
        return arr


        
        


        
            
                
