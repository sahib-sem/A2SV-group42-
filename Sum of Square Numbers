class Solution:
    def judgeSquareSum(self, c: int) -> bool:
        sqrt_c=math.floor(math.sqrt(c))
        for i in range(sqrt_c,-1,-1):
            b_square=c-i*i
            b=math.sqrt(b_square)    
            if int(b)==b:
                return True
        return False

            
