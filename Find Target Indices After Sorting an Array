class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        nums.sort()
        res=[]
        for i,value in enumerate(nums):
            if value==target:
                res.append(i)
        return res
